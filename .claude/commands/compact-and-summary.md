---
description: Perform context compaction for the current session and output the stats summary of how much space have been freed
---

## Your task:
Perform the built-in `/context`, `/compact` commands in a row in the following order:
`/context` -> `/compact` -> `/context`

And then compare the `Estimated usage by category` field in each `/context` command output before/after `/compact` runs, and then output the stats summary of how much space has been cleared.
